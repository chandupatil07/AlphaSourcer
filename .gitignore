node_modules/
.next/
dist/
build/
*.log
.env.local
.env*.local
.DS_Store
*.pem
.vercel

# Runtime search sessions
.sessions/

# TypeScript incremental build cache
*.tsbuildinfo

# scratch for one-off measurements; the harnesses that matter are committed
eval/_tmp/

# --- secrets: backups and copies ---
# `.env.local` was already ignored, but a timestamped backup of it
# (.env.local.bak-2026-09-23) was NOT, and `git add -A` staged it for commit
# with live Groq and Serper keys inside. Ignore every variant, not just the
# exact filename.
.env.local.*
.env.*.bak
.env.bak*
*.env.local*
